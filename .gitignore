# Dependencies
node_modules/
.pnpm-store/

# Next.js
.next/
out/
next-env.d.ts

# Env files — never commit secrets.
# Broad match with an explicit exception, so .env.production (and anything else
# added later) can't slip through the way a fixed list would.
.env
.env.*
!.env.example

# Prisma
# migrations/ is intentionally tracked

# Build artefacts
dist/
build/

# OS / IDE
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
pino-*.ndjson

# Vitest
coverage/

# TypeScript build info
tsconfig.tsbuildinfo
