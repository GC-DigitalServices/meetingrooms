// Display layout — full-screen iPad UI, no navigation chrome.
// Device token auth and room resolution added in Phase 6.
export default function DisplayLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
