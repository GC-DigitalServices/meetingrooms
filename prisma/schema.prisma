// Phase 1 — minimal schema, datasource and generator only.
// The full data model is added in Phase 2.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
