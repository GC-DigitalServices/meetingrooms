// Portal layout — staff and student booking experience.
// MSAL authentication, shadcn/ui, full navigation added in Phase 3+.
export default function PortalLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
